import { ClientBulkSms } from "@/components/client/client-bulk-sms";

export const metadata = {
  title: "Bulk SMS · LeadVon Client",
};

export default function ClientBulkSmsPage() {
  return <ClientBulkSms />;
}
