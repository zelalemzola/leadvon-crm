import { ClientCallScripts } from "@/components/client/client-call-scripts";

export const metadata = {
  title: "Call Scripts · LeadVon Client",
};

export default function ClientCallScriptsPage() {
  return <ClientCallScripts />;
}
