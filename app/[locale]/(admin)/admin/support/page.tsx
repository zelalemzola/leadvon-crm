import { AdminSupport } from "@/components/admin/admin-support";

export const metadata = {
  title: "Support · LeadVon Admin",
};

export default function AdminSupportPage() {
  return <AdminSupport />;
}
